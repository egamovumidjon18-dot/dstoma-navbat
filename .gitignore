node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Android/Capacitor build output — the android/ project itself is source-controlled,
# but Gradle's generated build artifacts and local machine paths are not.
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/*.iml
android/.idea/
